<header id="header">
  <div class="row">
    <div class="columns">
      <a href="/" id="logo"><img src="/assets/logo.png"></a>
    </div>
    <div class="small-9 columns" id="navBar">
      <nav id="links" class="show-for-medium">
        <% @links.each do |link| %>
          <a href="<%= link[:url] %>"><%= link[:name] %></a>
        <% end %>
      </nav>
      <%= content_tag :div,
        id: "toggle",
        data: {
          links: @links,
        }.to_json do %>
      <% end %>
    </div>
  </div>
</header>
